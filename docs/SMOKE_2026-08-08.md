# 2026-08-08 smoke follow-up

> Történeti smoke-jelentés. Az itt leírt direct/shadow futtatók már nem részei
> az aktuális repónak.

The 10-minute three-source run `20260808-073351-422-c629b89d` completed without
process, container, or test stderr failures, but it was not an acceptance pass.

## Findings

- SharpX headless reference: `0/778` initialized markets; direct collector was
  healthy. The root cause was a protocol-shape regression: `bdatl[0]` is an
  object containing `odds` and `amount`, not a numeric value. The browser
  collector now validates `bestLay.odds`, and the regression fixture uses the
  real object shape.
- Vegas direct-only events were a downstream effect of the empty canonical
  SharpX watchlist. The Vegas matching implementation was not changed. The
  common live-event odds spikes reverted during the run while status, live
  phase, and start time remained equal.
- TippmixPro snapshots now publish `pendingWorkDetails` and an explicit
  `snapshotConsistency` marker. Comparator and production health gates accept
  consistent snapshots during background WAMP work, reject malformed or
  inconsistent markers, and keep the conservative pending-work gate for legacy
  snapshots without the marker.

## 15-minute follow-up smoke

Run `20260808-091257-239-5119ee1a` completed on the PIA Docker route. The
launcher watchdog wrote `status: completed`, `completedAt`, and
`cleanupCompletedAt`; all six tracked processes and all three run containers
were gone after cleanup.

- SharpX: 670/825 eligible samples (81.21%), 778/783 markets at the final
  sample, zero persistent presence or odds evidence; invalid samples were
  snapshot-skew only.
- Vegas: 816/831 eligible samples (98.19%), 99.835% odds agreement on
  562,342 common-event observations; the remaining invalid samples were short
  live-refresh freshness windows.
- TippmixPro: 814/814 eligible samples (100%), no invalid samples and no
  pending-work or snapshot-consistency rejections; odds agreement was 99.95%
  over 1,337,979 common-event observations.

All collector and comparator stderr logs were empty. The pre-existing headless
reference stack remained running and continued publishing fresh output.
